"""IP3 page modules. Each module exposes a ``render()`` function.

These are invoked by the in-app router in ``app.py`` (not Streamlit's
automatic multipage navigation, which is disabled in config).
"""
